import type { NavSectionProps } from 'src/components/nav-section';

// ----------------------------------------------------------------------

/** Legacy export kept for compatibility after removing the teacher routes. */
export const navData: NavSectionProps['data'] = [];
