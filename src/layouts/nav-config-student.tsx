import type { NavMainProps } from './main/nav/types';

// ----------------------------------------------------------------------

/** Legacy export kept for compatibility after removing the student routes. */
export const studentNavData: NavMainProps['data'] = [];
